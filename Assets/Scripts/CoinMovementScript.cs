using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoinMovementScript : MonoBehaviour {

	GameObject ball = GameObject.Find("BallSprite");
//	MovementScript m = ball.GetComponent<MovementScript>();

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	//	float movement = (1f - m.movement);
//		Rigidbody2D rb = GetComponent<Rigidbody2D> ();
//		rb.velocity = new Vector2 (movement * 5, rb.velocity.y);
		//ball.GetComponent<MovementScript>().
	}
}
